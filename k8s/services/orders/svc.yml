apiVersion: v1
kind: Service
metadata:
  name: orders
  namespace: mv100
spec:
  selector:
    app: orders
  ports:
    - port: 3003
      targetPort: 3003
---
apiVersion: v1
kind: Service
metadata:
  name: orders-canary
  namespace: mv100
spec:
  selector:
    app: orders
  ports:
    - port: 3003
      targetPort: 3003
