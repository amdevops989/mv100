apiVersion: v1
kind: Service
metadata:
  name: cart
  namespace: mv100
spec:
  selector:
    app: cart
  ports:
    - port: 3002
      targetPort: 3002
---
apiVersion: v1
kind: Service
metadata:
  name: cart-canary
  namespace: mv100
spec:
  selector:
    app: cart
  ports:
    - port: 3002
      targetPort: 3002
